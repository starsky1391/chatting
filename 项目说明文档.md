# 聊天应用项目说明文档

## 项目结构

### 后端项目结构 (backend/)
```
├── src/
│   ├── config/
│   │   ├── database.ts          # PostgreSQL数据库配置
│   │   └── index.ts              # 主配置文件（端口、JWT、CORS等）
│   ├── controllers/              # 控制器层（处理HTTP请求）
│   │   ├── AuthController.ts     # 认证相关控制器
│   │   ├── ChannelController.ts  # 频道管理控制器
│   │   └── MessageController.ts  # 消息管理控制器
│   ├── middleware/
│   │   └── auth.ts               # JWT认证中间件
│   ├── models/                   # 数据模型层
│   │   ├── Channel.ts            # 频道模型
│   │   ├── Message.ts            # 消息模型
│   │   └── User.ts               # 用户模型
│   ├── routes/
│   │   └── index.ts              # API路由定义
│   ├── services/                 # 业务逻辑层
│   │   ├── AuthService.ts        # 认证服务
│   │   ├── ChannelService.ts     # 频道服务
│   │   └── MessageService.ts     # 消息服务
│   ├── utils/
│   │   ├── initDatabase.ts       # 数据库初始化脚本
│   │   ├── createDatabase.ts     # 数据库创建脚本
│   │   └── response.ts           # 统一API响应格式
│   └── index.ts                  # 应用入口文件
├── .env                          # 环境变量配置
├── .env.example                  # 环境变量示例
├── package.json                  # 项目依赖和脚本
└── tsconfig.json                 # TypeScript配置
```

### 前端项目结构 (root/)
```
├── public/                       # 静态资源
├── src/
│   ├── app/                      # Next.js App Router页面
│   │   ├── login/                # 登录页面
│   │   ├── register/             # 注册页面
│   │   └── page.tsx              # 主聊天页面
│   ├── components/               # 组件
│   │   ├── MainLayout.tsx        # 主布局组件
│   │   ├── members/              # 成员相关组件
│   │   │   └── MemberList.tsx     # 成员列表组件
│   │   ├── messages/             # 消息相关组件
│   │   │   ├── MessageArea.tsx   # 消息区域组件
│   │   │   └── MessageBubble.tsx # 消息气泡组件
│   │   └── sidebar/              # 侧边栏组件
│   │       └── ChannelList.tsx   # 频道列表组件
│   ├── hooks/                    # 自定义钩子
│   │   └── useSocket.ts          # Socket.io钩子
│   ├── store/                    # 状态管理
│   │   └── useChatStore.ts       # 聊天状态
│   ├── lib/                      # 工具函数
│   │   ├── utils.ts              # 通用工具
│   │   ├── socket.ts             # Socket.io配置
│   │   └── config.ts             # 前端配置管理
│   ├── .env.local                # 前端环境变量配置
│   └── .env.local.example        # 前端环境变量示例
├── package.json                  # 项目依赖
└── tsconfig.json                 # TypeScript配置
```

## 文件功能说明

### 后端核心文件

| 文件路径 | 功能说明 |
|---------|----------|
| `src/index.ts` | 应用入口，初始化Express、Socket.io和路由 |
| `src/config/database.ts` | PostgreSQL数据库连接配置 |
| `src/models/User.ts` | 用户数据模型，定义用户表结构 |
| `src/models/Message.ts` | 消息数据模型，定义消息表结构 |
| `src/models/Channel.ts` | 频道数据模型，定义频道表结构 |
| `src/controllers/AuthController.ts` | 处理登录、注册等认证请求 |
| `src/controllers/MessageController.ts` | 处理消息相关请求 |
| `src/services/AuthService.ts` | 认证业务逻辑，JWT生成和验证 |
| `src/middleware/auth.ts` | JWT认证中间件，保护API路由 |
| `src/utils/response.ts` | 统一API响应格式 |
| `src/utils/initDatabase.ts` | 数据库模型同步脚本 |

### 前端核心文件

| 文件路径 | 功能说明 |
|---------|----------|
| `src/app/page.tsx` | 主聊天页面，显示消息列表 |
| `src/app/login/page.tsx` | 用户登录页面 |
| `src/app/register/page.tsx` | 用户注册页面 |
| `src/components/MainLayout.tsx` | 应用主布局，包含侧边栏和消息区域 |
| `src/components/messages/MessageBubble.tsx` | 消息气泡组件 |
| `src/store/useChatStore.ts` | 聊天状态管理 |
| `src/hooks/useSocket.ts` | Socket.io实时通信钩子 |
| `src/lib/config.ts` | 前端配置管理，包含API地址、Socket地址和WebRTC配置 |
| `src/lib/socket.ts` | Socket.io连接配置和管理 |
| `src/components/voice/VoiceRoom.tsx` | 语音通话功能组件，包含WebRTC连接管理和音频控制 |

## 技术栈

### 后端
- Node.js + Express
- TypeScript
- Socket.io（实时通信）
- PostgreSQL + Sequelize ORM
- JWT认证
- bcryptjs（密码哈希）

### 前端
- Next.js 14
- TypeScript
- React
- Socket.io Client
- Zustand（状态管理）
- Tailwind CSS
- Markdown支持

## 开发流程

1. **后端开发**：
   - 安装依赖：`npm install`
   - 配置环境变量：复制 `.env.example` 为 `.env`
   - 创建数据库：`npm run db:create`
   - 初始化模型：`npm run db:init`
   - 启动开发服务器：`npm run dev`

2. **前端开发**：
   - 安装依赖：`npm install`
   - 配置环境变量：复制 `.env.local.example` 为 `.env.local`
   - 启动开发服务器：`npm run dev`
   - 访问：http://localhost:3000

## 主要功能

1. **用户管理**：注册、登录、身份验证
2. **频道管理**：创建、查询、加入频道
3. **消息系统**：发送、接收、历史消息
4. **实时通信**：Socket.io双向通信
5. **成员管理**：在线状态、角色权限
6. **响应式设计**：适配移动端和桌面端
7. **语音通话**：基于WebRTC的实时语音通话功能，支持音量调节和静音控制
8. **配置管理**：集中式配置管理，支持环境变量和多环境部署

## 注意事项

- 确保PostgreSQL服务已启动
- 后端默认端口：3001
- 前端默认端口：3000
- 数据库连接信息在 `backend/.env` 文件中配置
- JWT密钥在 `backend/.env` 中配置，生产环境需使用强密钥
- 前端API和Socket地址在 `.env.local` 文件中配置
- WebRTC ICE Server配置在 `src/lib/config.ts` 中管理
- 生产环境部署时，请修改环境变量中的服务器地址为实际生产地址

## API文档

详细API文档见 `api-docs.json` 文件，包含所有API端点、参数和响应格式。